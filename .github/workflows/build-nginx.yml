name: Build and Push Nginx Images

on:
  push:
    branches: [main, develop]
    paths:
      - 'nginx/**'
      - '.github/workflows/build-nginx.yml'
    tags:
      - 'nginx-*'
  pull_request:
    branches: [main]
    paths:
      - 'nginx/**'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/nginx

jobs:
  build-matrix:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        os_variant: ['alpine', 'bookworm', 'trixie']

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Determine Dockerfile path
        id: dockerfile
        run: |
          # Map os_variant to Dockerfile path
          # Context is always repo root because Dockerfiles reference paths like nginx/common/
          case "${{ matrix.os_variant }}" in
            alpine)
              echo "path=./nginx/alpine/Dockerfile" >> $GITHUB_OUTPUT
              ;;
            bookworm)
              echo "path=./nginx/debian/bookworm/Dockerfile" >> $GITHUB_OUTPUT
              ;;
            trixie)
              echo "path=./nginx/debian/trixie/Dockerfile" >> $GITHUB_OUTPUT
              ;;
          esac
          echo "context=." >> $GITHUB_OUTPUT

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: network=host

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch,suffix=-${{ matrix.os_variant }}
            type=ref,event=pr,suffix=-${{ matrix.os_variant }}
            type=semver,pattern={{version}},suffix=-${{ matrix.os_variant }}
            type=semver,pattern={{major}}.{{minor}},suffix=-${{ matrix.os_variant }}
            type=raw,value=${{ matrix.os_variant }},enable={{is_default_branch}}
            type=raw,value=latest,enable=${{ matrix.os_variant == 'alpine' && github.ref == 'refs/heads/main' }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ${{ steps.dockerfile.outputs.context }}
          file: ${{ steps.dockerfile.outputs.path }}
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=nginx-${{ matrix.os_variant }}
          cache-to: type=gha,mode=max,scope=nginx-${{ matrix.os_variant }}

      - name: Test image
        if: github.event_name != 'pull_request'
        run: |
          docker run -d --name test-nginx -p 8080:80 ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ matrix.os_variant }}
          sleep 5
          curl -f http://localhost:8080/health || exit 1
          docker stop test-nginx && docker rm test-nginx
